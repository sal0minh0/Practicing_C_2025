#include <stdio.h>

int main () {
    int x, y, z, result;

    printf("Product of two numbers, Insert them: ");
    scanf("%d %d %d", &x, &y, &z);

    result = x * y * z;

    printf ("The Result is: %d\n", result);
     
}