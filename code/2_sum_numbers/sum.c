#include <stdio.h>

int main(void) {
	int number1, number2;
	
	printf("Doing the SUM of two numbers, Insert them NOW: ");
	scanf("%d %d", &number1, &number2);

	printf("The SUM is %d\n", number1 + number2);

	return 0;
}
